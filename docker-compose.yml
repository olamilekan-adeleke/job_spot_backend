version: '3'
services:
  api:
    image: olakod/job_spot_backend
    environment:
      DATABASE_URL: "postgres://postgres:${POSTGRES_PASSWORD}@db:5433"
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
  #   depends_on:
  #     - db
  #
  # db:
  #   image: postgres:15.2-alpine
  #   restart: always
  #   environment:
  #     POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
  #   ports:
  #     - "5433:5433"
  #   volumes:
  #     - db:/var/lib/postgresql/data

volumes:
  db:
    driver: local
